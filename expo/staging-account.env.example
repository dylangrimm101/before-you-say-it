# Shell-only example, NOT an Expo dotenv file. Never put credentials/admin keys here.
# The key must come from project pqqxaklcburdxjfeolmd. Publishable keys are public.
export EXPO_PUBLIC_STAGING_SUPABASE_URL="https://pqqxaklcburdxjfeolmd.supabase.co"
export EXPO_PUBLIC_STAGING_SUPABASE_PUBLISHABLE_KEY="REPLACE_WITH_STAGING_PUBLISHABLE_KEY"
# Optional, separately reviewed acquisition opt-in; disabled when omitted.
# This does not enable anonymous Auth, create an account, or authorize provider calls.
# export EXPO_PUBLIC_STAGING_FREE_GENERATE_ENDPOINT="https://bysi-signup-staging.vercel.app/api/web-signup/generate"
# run-staging-account.ts sets the explicit build-mode flag and disables dotenv itself.
