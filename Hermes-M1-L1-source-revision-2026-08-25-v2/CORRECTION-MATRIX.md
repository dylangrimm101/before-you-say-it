# Hermes correction matrix

1. Temporary recording cleanup: native and browser stop/release/delete paths fail closed across reset, cancel, unmount, transcription, safety exit, and discard.
2. Completion recovery: versioned phase-aware journal records strict private deletion before active-key removal and promotes idempotently after restart.
3. Environment lifecycle: canonical guard command is wired before start, test, check, lint, and export scripts.
4. Persisted run integrity: nested attempts, pressure fields, timestamps, flags, and replay proof are strictly parsed and canonically reconstructed; malformed storage becomes a content-free tombstone.
5. Universal active-run CAS: the non-CAS writer is removed; creation and all shared/converted mutations use serialized revision-bound operations with interleaved cross-practice tests.
6. Card 21: durable approved-move state transforms both the real action closure and navigation gate; empty optional blanks behaviorally advance to Card 22.
7. Final retry: capture stays locked in replay-pending until playback starts or the learner explicitly acknowledges the exact text fallback.
8. Coaching replay: opener coaching uses a top-of-scene reset; Beat 3 replays Pushback 1; Beat 5 replays the evidence trap.
9. Contextual audio: a persona is bound to the run and passed through each real shared playback invocation for work, partner, family, and friends.
10. Evidence quotes: quotes preserve exact source whitespace/punctuation and contiguous offsets; unsupported dimensions return `not_assessable` with no quote.
11. Behavioral tests: failure, crash, malformed JSON, race, Card 21 action, replay proof, all coached beats, contextual playback, and exact evidence cases are executed.
12. Package hygiene: generated `.expo` cache material is excluded.

Previously accepted Hope transitions, work/Adam isolation, semantic voice mapping, stable audio IDs, Beat 7, direct-question classification, incoming-progress normalization, and exact `[21, 22]` inventory remain preserved.
