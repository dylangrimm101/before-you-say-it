# Correction Matrix

1. Temporary and retained audio deletion now has strict, verified, throwing variants; privacy deletion flows do not confirm success after a filesystem failure.
2. Completion is journaled as non-visible pending → strict private-content deletion → visible promotion, with restart recovery.
3. Client environment handling uses an explicit allowlist, clears inherited process variables, rejects unknown secret-like public names, and runs before Metro, Babel/export, and lint/check configuration.
4. Hope comparison implements every met/not-met/not-assessable transition explicitly.
5. Restored M1 L1 pressure turns validate canonical text, reaction, semantic voice, stable audio ID, run-bound turn ID, timestamps, and order.
6. Strict active-run replace, clear, and archive operations are serialized compare-and-swap operations bound to run ID and updatedAt.
7. Card 21 can advance from durable approved named-move persistence while custom blanks remain optional; the return inventory must be exactly Cards 21–22.
8. Final-retry capture is unavailable until the exact selected pressure is staged and its identity is persisted.
9. Hope selects the highest-priority failed behavior across the opener, Beat 3, and Beat 5 and persists the evidence-carrying beat.
10. Shared scenarios resolve contextual counterpart voice at both playback boundaries; isolated M1 L1 remains Adam.
11. Behavior flags use exact contiguous, dimension-specific evidence sentences.
12. Resume uses the strict accepted M1 L1 helper, including work-category and Adam identity.
13. Semantic pilot voice mapping is explicit; unresolved contextual voice fails closed rather than defaulting to Adam.
14. Authored audio cache IDs use content version + pressure identity + semantic voice and no longer include run ID.
15. Beat 7 is persisted when exact replay is staged and is visible through the shared progress state.
16. A direct question is no longer classified as indirect solely because it contains a question mark.
17. Incoming converted progress is normalized and rejected before merge persistence.
18. Returned deck transformation verifies exact `[21, 22]` inventory.
19. Deterministic failure, race, crash, transition, tamper, replay, voice, and inventory tests were added and the full validation stack was rerun.
